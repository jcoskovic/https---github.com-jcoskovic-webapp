# Railway ignore file
# Force Railway to use Dockerfile build instead of auto-detection

# Ignore lock files to force fresh install
**/package-lock.json
**/node_modules
.git
.github
scripts/
.env*

# Let Railway auto-detect services from:
# - backend/ (Laravel)
# - frontend/ (Angular)  
# - ml-service/ (Python Flask)